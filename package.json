{
  "name": "github-agent",
  "version": "1.0.0",
  "description": "GitHub automation agent with retry mechanisms and error handling",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest -c jest.integration.config.js",
    "test:all": "npm run test && npm run test:integration"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "dotenv": "^16.4.1",
    "jest": "^29.7.0"
  }
}